kind: pipeline
type: docker
name: default

platform:
  os: linux
  arch: arm64

steps:
- name: pytest
  image: python:3.10-bullseye
  commands:
  - pip install -r requirements.txt
  - cd djangoapp
  - pytest --import-mode=importlib